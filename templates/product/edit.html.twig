{% extends 'base.html.twig' %}

{% block title %}{{ "Edit Product"|trans }}{% endblock %}

{% block body %}
    <h1>{{ "Edit Product"|trans }}</h1>
    <div class="row">
        <div class="col"><a class="btn btn-link float-left" href="{{ path('product_index') }}">{{ "Back to list" |trans }}</a>{{ include('product/_delete_form.html.twig') }}</div>
    </div>

    {{ include('product/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('product') }}
{% endblock %}
