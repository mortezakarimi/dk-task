{% extends 'base.html.twig' %}

{% block title %}{{ "New Product"|trans }}{% endblock %}

{% block body %}
    <h1>{{ "Create new Product" | trans }}</h1>
    <a class="btn btn-link float-left" href="{{ path('product_index') }}">{{ "Back to list" |trans }}</a>
    <div class="clearfix"></div>
    {{ include('product/_form.html.twig') }}
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('product') }}
{% endblock %}
